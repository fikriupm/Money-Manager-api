#MySql Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/moneymanager
spring.datasource.username=root
spring.datasource.password=password123

server.servlet.context-path=/api/v1.0

#JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#Email Configuration
spring.mail.host=smtp-relay.brevo.com
spring.mail.port=587
# Mail credentials should come from environment variables (do NOT commit secrets)
spring.mail.username=${BREVO_USERNAME}
spring.mail.password=${BREVO_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.from=${BREVO_FROM_EMAIL:no-reply@money-manager.local}

# Enable/disable email sending (can be set via env var MAIL_ENABLED)
app.mail.enabled=${MAIL_ENABLED:true}

jwt.secret=-2b8e7c7e-4e2a-4b1a-9c2e-8f7e2d3c4b5a

money.manager.frontend.url=${MONEY_MANAGER_FRONTEND_URL:http://localhost:5173}

# Activation URL used in emails; set in your environment on production
app.activation.url=${MONEY_MANAGER_BACKEND_URL:http://localhost:8080}

spring.profiles.active=prod



